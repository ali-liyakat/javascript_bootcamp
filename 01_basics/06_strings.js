const myname="ali"      // we can use single or double quotes.
const age=22;           // ** -> string is an object

console.log(`My name is ${myname} and my age is ${age}`);   //modern way

const userName= new String("Liyakat");      // string object
// we can access the string values using index notation []

// ******** String Methods ***************************
// use dot notation to acces the string methods.

console.log(userName.length);
//  .charAt(), .indexOf(), .substring(), .slice(), .trim(), .replace(), .includes(), .split()
// MDN docs for various string methods.
